#include <iostream>

using namespace std;

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    int a = 0, b = 0;
    cin >> a >> b;
    cout << (a * 10 + b) / (1 * 10 + 9) << endl;
    return 0;
}